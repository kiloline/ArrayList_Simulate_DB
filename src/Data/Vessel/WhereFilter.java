package Data.Vessel;

public class WhereFilter extends Word {
    public WhereFilter(String c_name, String c_substance, int line, int list, boolean ismark) {
        super(c_name, c_substance, line, list, ismark);
    }
}
